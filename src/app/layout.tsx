import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "Dynamic Portfolio Dashboard",
  description: "Real-time portfolio dashboard with Yahoo Finance and Google Finance data",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} min-h-screen bg-slate-100 font-sans antialiased`}>
        {children}
      </body>
    </html>
  );
}
